<table border="1">
  <tr>
    <th>Latitude</th>
    <th>Longitude</th>
    <th>Date</th>
  </tr>
<% @locations.each do |l| %>
  <tr>
    <td><%= l.latitude %></td>
    <td><%= l.longitude %></td>
    <td><%= l.created_at %></td>
  </tr>
<% end %>
</table>