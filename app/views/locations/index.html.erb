<!DOCTYPE html>
<html>

  <head>
    <title>Tracerapp</title>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="UTF-8">
  </head>
  
  <body>
  <% @locations.each do |l| %>
  <h3>Latitude: <%= l.latitude %></h3>
  <h3>Longitude: <%= l.longitude %></h3>
  <% end %>
  <%= link_to 'Your location?', new_location_path %>
  <p>
  Download:
  <%= link_to "CSV", locations_path(format: "csv") %>
  <%= link_to "XLS", locations_path(format: "xls")%>
  </p>
    
  </body>
</html>